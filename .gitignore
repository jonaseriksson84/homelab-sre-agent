# Binaries
/sre-agent
/bin/
*.exe

# Test artifacts
*.test
coverage.out
coverage.html

# SQLite databases (local runs)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Local environment / secrets
.env
.env.*

# Local scratch (issue-tracker convention keeps .scratch/ untracked)
.scratch/

# Operator-local records: the dev diary and the maintainer's own deployment
# notes stay on disk but out of the public repo.
/implementation-notes.md
/docs/deployment.tower.md

# Editor / OS noise
.DS_Store
.idea/
.vscode/
